import { cn } from './util/cn'

function App() {
    return (
        <div
            className={cn(
                'flex',
                'h-screen',
                'items-center',
                'justify-center',
                'w-screen',
            )}
        >
            Hello World
        </div>
    )
}

export default App
